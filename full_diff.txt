diff --git a/src/components/shared/shared-avatar.tsx b/src/components/shared/shared-avatar.tsx
index b97b55c..85682da 100644
--- a/src/components/shared/shared-avatar.tsx
+++ b/src/components/shared/shared-avatar.tsx
@@ -11,6 +11,7 @@ interface BaseAvatarProps {
     size?: AvatarSize;
     backgroundColor?: string;
     bgColor?: ThemeColor;
+    accessibilityLabel?: string;
 }
 
 interface AvatarWithIcon extends BaseAvatarProps {
@@ -38,7 +39,7 @@ const AVATAR_SIZES = {
 
 export function SharedAvatar(props: SharedAvatarProps) {
     const theme = useTheme();
-    const { size = 'medium', backgroundColor, bgColor } = props;
+    const { size = 'medium', backgroundColor, bgColor, accessibilityLabel } = props;
 
     const sizeConfig = AVATAR_SIZES[size];
     const containerSize = sizeConfig.container;
@@ -49,6 +50,7 @@ export function SharedAvatar(props: SharedAvatarProps) {
     if (props.variant === 'icon') {
         const { iconName, iconColor, iconThemeColor } = props;
         const resolvedIconColor = iconColor || (iconThemeColor ? theme.colors[iconThemeColor] : theme.colors.white);
+        const a11yLabel = accessibilityLabel || `Icon ${iconName}`;
 
         return (
             <View
@@ -61,6 +63,9 @@ export function SharedAvatar(props: SharedAvatarProps) {
                         backgroundColor: resolvedBgColor
                     }
                 ]}
+                accessible={true}
+                accessibilityRole="image"
+                accessibilityLabel={a11yLabel}
             >
                 <Icon
                     variant="material-community"
@@ -75,6 +80,7 @@ export function SharedAvatar(props: SharedAvatarProps) {
     // variant === 'text'
     const { text, textColor, textThemeColor } = props;
     const resolvedTextColor = textColor || (textThemeColor ? theme.colors[textThemeColor] : theme.colors.white);
+    const a11yLabel = accessibilityLabel || `Avatar with initials ${text}`;
 
     return (
         <View
@@ -87,6 +93,9 @@ export function SharedAvatar(props: SharedAvatarProps) {
                     backgroundColor: resolvedBgColor
                 }
             ]}
+            accessible={true}
+            accessibilityRole="image"
+            accessibilityLabel={a11yLabel}
         >
             <SharedText
                 variant="label"
diff --git a/src/components/shared/shared-button.tsx b/src/components/shared/shared-button.tsx
index 29d0010..7667c02 100644
--- a/src/components/shared/shared-button.tsx
+++ b/src/components/shared/shared-button.tsx
@@ -26,6 +26,8 @@ export interface SharedButtonProps {
     leftIcon?: IconSymbolName | ButtonIconConfig | ReactNode;
     style?: ViewStyle;
     textStyle?: TextStyle;
+    accessibilityLabel?: string;
+    accessibilityHint?: string;
 }
 
 export function SharedButton({
@@ -37,7 +39,9 @@ export function SharedButton({
     children,
     leftIcon,
     style,
-    textStyle
+    textStyle,
+    accessibilityLabel,
+    accessibilityHint
 }: SharedButtonProps) {
     const theme = useTheme();
 
@@ -158,12 +162,22 @@ export function SharedButton({
         return leftIcon as ReactNode;
     };
 
+    const buttonLabel = accessibilityLabel || (typeof children === 'string' ? children : 'Button');
+    const loadingLabel = loading ? `${buttonLabel}, loading` : buttonLabel;
+
     return (
         <TouchableOpacity
             onPress={onPress}
             style={buttonStyles}
             activeOpacity={0.7}
             disabled={disabled || loading}
+            accessibilityRole="button"
+            accessibilityLabel={loadingLabel}
+            accessibilityHint={accessibilityHint}
+            accessibilityState={{
+                disabled: disabled || loading,
+                busy: loading
+            }}
         >
             {loading ? (
                 <ActivityIndicator color={iconColor} />
diff --git a/src/components/shared/shared-input.tsx b/src/components/shared/shared-input.tsx
index f3a9ba5..042bb5e 100644
--- a/src/components/shared/shared-input.tsx
+++ b/src/components/shared/shared-input.tsx
@@ -9,16 +9,22 @@ export interface SharedInputProps extends Omit<TextInputProps, 'style'> {
     error?: boolean;
     errorMessage?: string;
     containerStyle?: any;
+    accessibilityLabel?: string;
+    accessibilityHint?: string;
 }
 
 export const SharedInput = forwardRef<TextInput, SharedInputProps>(
-    ({ label, helperText, error = false, errorMessage, containerStyle, ...textInputProps }, ref) => {
+    ({ label, helperText, error = false, errorMessage, containerStyle, accessibilityLabel, accessibilityHint, ...textInputProps }, ref) => {
         const theme = useTheme();
 
         const borderColor = error ? theme.colors.slNegative : theme.colors.slBorder;
         const helperColor = error ? theme.colors.slNegative : theme.colors.slTextMuted;
         const displayHelperText = error && errorMessage ? errorMessage : helperText;
 
+        // Accessibility label generation
+        const inputLabel = accessibilityLabel || label || textInputProps.placeholder || 'Input field';
+        const inputHint = accessibilityHint || displayHelperText || undefined;
+
         return (
             <View style={[styles.container, containerStyle]}>
                 {label && (
@@ -49,6 +55,10 @@ export const SharedInput = forwardRef<TextInput, SharedInputProps>(
                             }
                         ]}
                         placeholderTextColor={theme.colors.slTextMuted}
+                        accessibilityLabel={inputLabel}
+                        accessibilityHint={inputHint}
+                        accessibilityRole="none"
+                        accessible={true}
                         {...textInputProps}
                     />
                 </View>
diff --git a/src/components/shared/shared-text.tsx b/src/components/shared/shared-text.tsx
index 6c765df..86f7779 100644
--- a/src/components/shared/shared-text.tsx
+++ b/src/components/shared/shared-text.tsx
@@ -11,6 +11,8 @@ export interface SharedTextProps {
     style?: TextStyle;
     children: ReactNode;
     numberOfLines?: number;
+    accessibilityLabel?: string;
+    accessibilityRole?: 'header' | 'text' | 'none';
 }
 
 export function SharedText({
@@ -19,7 +21,9 @@ export function SharedText({
     align = 'auto',
     style,
     numberOfLines,
-    children
+    children,
+    accessibilityLabel,
+    accessibilityRole
 }: SharedTextProps) {
     const theme = useTheme();
     const variantStyle = theme.typography.variants[variant];
@@ -38,8 +42,17 @@ export function SharedText({
         style
     ];
 
+    // Determine accessibility role based on variant if not specified
+    const defaultRole = variant === 'h1' || variant === 'h2' ? 'header' : undefined;
+    const role = accessibilityRole !== undefined ? accessibilityRole : defaultRole;
+
     return (
-        <Text style={textStyle} numberOfLines={numberOfLines}>
+        <Text
+            style={textStyle}
+            numberOfLines={numberOfLines}
+            accessibilityLabel={accessibilityLabel}
+            accessibilityRole={role as any}
+        >
             {children}
         </Text>
     );
diff --git a/src/features/add-trusted-heir/add-trusted-heir.tsx b/src/features/add-trusted-heir/add-trusted-heir.tsx
index 43a4a9d..d316e29 100644
--- a/src/features/add-trusted-heir/add-trusted-heir.tsx
+++ b/src/features/add-trusted-heir/add-trusted-heir.tsx
@@ -60,6 +60,8 @@ const AddTrustedHeir = () => {
                         autoCapitalize='none'
                         autoCorrect={false}
                         editable={!isSubmitting}
+                        accessibilityLabel="Guardian's email address"
+                        accessibilityHint="Enter the email address of your recovery guardian"
                     />
                 </View>
 
@@ -86,6 +88,8 @@ const AddTrustedHeir = () => {
                             color: theme.colors.white
                         }
                     }}
+                    accessibilityLabel={t('addHeir.submit')}
+                    accessibilityHint="Issues a recovery permission credential to the guardian"
                 >
                     {t('addHeir.submit')}
                 </SharedButton>
diff --git a/src/features/add-trusted-heir/components/intro-section.tsx b/src/features/add-trusted-heir/components/intro-section.tsx
index c100bb6..f6a3dc9 100644
--- a/src/features/add-trusted-heir/components/intro-section.tsx
+++ b/src/features/add-trusted-heir/components/intro-section.tsx
@@ -19,12 +19,14 @@ export function IntroSection({ iconName, title, description }: IntroSectionProps
                 size="large"
                 bgColor="slAccentLight"
                 iconThemeColor="slAccent"
+                accessibilityLabel="Recovery guardian setup"
             />
             <SharedText
                 variant="h2"
                 color="slText"
                 align="center"
                 style={styles.title}
+                accessibilityRole="header"
             >
                 {title}
             </SharedText>
diff --git a/src/features/home/components/balance-section.tsx b/src/features/home/components/balance-section.tsx
index 1974687..a10feec 100644
--- a/src/features/home/components/balance-section.tsx
+++ b/src/features/home/components/balance-section.tsx
@@ -21,6 +21,7 @@ export function BalanceSection({ totalBalance, usdcBalance }: BalanceSectionProp
                 variant='h1'
                 color='slText'
                 style={{ fontSize: 32, fontWeight: '300', letterSpacing: -0.5 }}
+                accessibilityLabel={`Total balance: ${totalBalance.replace(/\$/g, '').replace(/,/g, '')} dollars`}
             >
                 {totalBalance}
             </SharedText>
diff --git a/src/features/home/components/detail-row.tsx b/src/features/home/components/detail-row.tsx
index 0dd79f5..9ceba56 100644
--- a/src/features/home/components/detail-row.tsx
+++ b/src/features/home/components/detail-row.tsx
@@ -42,6 +42,9 @@ export function DetailRow({ label, value, iconName, onCopy }: DetailRowProps) {
                     <TouchableOpacity
                         onPress={onCopy}
                         hitSlop={{ top: 8, bottom: 8, left: 8, right: 8 }}
+                        accessibilityRole="button"
+                        accessibilityLabel={`Copy ${label}`}
+                        accessibilityHint="Copies to clipboard"
                     >
                         <Icon
                             variant='material-community'
diff --git a/src/features/home/components/guardian-card.tsx b/src/features/home/components/guardian-card.tsx
index d59e1db..6322e02 100644
--- a/src/features/home/components/guardian-card.tsx
+++ b/src/features/home/components/guardian-card.tsx
@@ -29,7 +29,13 @@ export function GuardianCard({ name, email, initials, isVerified, onManage }: Gu
                     {t('home.recoveryGuardian')}
                 </SharedText>
                 {onManage && (
-                    <TouchableOpacity activeOpacity={0.8} onPress={onManage}>
+                    <TouchableOpacity
+                        activeOpacity={0.8}
+                        onPress={onManage}
+                        accessibilityRole="button"
+                        accessibilityLabel={t('home.manage')}
+                        accessibilityHint="Manage recovery guardian settings"
+                    >
                         <SharedText
                             variant='label'
                             color='slAccent'
@@ -47,6 +53,7 @@ export function GuardianCard({ name, email, initials, isVerified, onManage }: Gu
                     size="medium"
                     bgColor="slAccentLight"
                     textThemeColor="slAccent"
+                    accessibilityLabel={`Guardian avatar: ${name}`}
                 />
                 <View style={styles.info}>
                     <SharedText
diff --git a/src/features/home/components/header-section.tsx b/src/features/home/components/header-section.tsx
index 0aed757..8328fe4 100644
--- a/src/features/home/components/header-section.tsx
+++ b/src/features/home/components/header-section.tsx
@@ -25,6 +25,7 @@ export function HeaderSection({ userName, userInitials, onNotificationPress }: H
                     size="medium"
                     bgColor="slAccent"
                     textThemeColor="white"
+                    accessibilityLabel={t('home.userAvatar', { name: userName })}
                 />
                 <View style={styles.greeting}>
                     <SharedText variant='caption' color='slTextMuted' style={{ fontSize: 13 }}>
@@ -43,6 +44,9 @@ export function HeaderSection({ userName, userInitials, onNotificationPress }: H
                 style={[styles.notifBtn, { backgroundColor: theme.colors.slSurface }]}
                 onPress={onNotificationPress}
                 activeOpacity={0.8}
+                accessibilityRole="button"
+                accessibilityLabel={t('home.notifications')}
+                accessibilityHint={t('home.notificationsHint')}
             >
                 <Icon
                     variant='material-community'
